# Neo4j Database Configuration
# Copy this file to .env and customize as needed

# Authentication
NEO4J_PASSWORD=changeme

# Ports (84xx range for Neo4j)
# To change environment (dev/test/staging), modify first 2 digits:
# Dev: 84xx | Test: 74xx | Staging: 94xx
NEO4J_HTTP_PORT=8474
NEO4J_BOLT_PORT=8487

# Memory Configuration (Realistic for 16-32GB systems)
# Heap: Java application memory
# Pagecache: File system cache for database files  
NEO4J_HEAP_MEMORY=512m
NEO4J_PAGECACHE_MEMORY=512m

# Environment Labels
ENVIRONMENT=dev
TEAM=platform
COMPOSE_PROJECT_NAME=neo4j-db